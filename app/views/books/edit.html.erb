<h1>Editing Book</h1>
<!-- form_for部分 -->
<%= form_for(@book) do |f| %>

	<label>Title</label>
	<%= f.text_field :title %>

	<label>Body</label>
	<%= f.text_field :body %><br>

	<%= f.submit 'Update Book' %>
<% end %>

<%= link_to "Show", book_path(@book) %>
<%= link_to "Back", books_path(@book) %>